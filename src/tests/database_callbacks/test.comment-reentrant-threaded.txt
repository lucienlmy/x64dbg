init tests/database_callbacks.exe

_c1 = database_callbacks:VariableTarget
_c2 = _c1 + 4

commentset _c1, old1
commentset _c2, old2
dbreset

dbthreadedreentrantcommentset _c1, reentrant-threaded
commentclear

// Callback delivery must not hold the writer lock while waiting for a worker.
assertopsize 3
assertlastop mod.hash(mod.base(_c1)), (_c1-mod.base(_c1)), c, a, 0, reentrant-threaded
