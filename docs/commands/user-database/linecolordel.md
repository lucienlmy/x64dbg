# linecolordel/linecolorclear

Remove the line color of an address. When called without arguments the line colors of the current disassembly selection are removed.

## arguments

`arg1` (optional) Address to remove the line color from.

## result

This command does not set any result variables.
